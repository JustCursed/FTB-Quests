package tech.funkyra.ftb.handlers;

import net.minecraftforge.fml.common.eventhandler.SubscribeEvent;
import net.minecraftforge.fml.common.gameevent.PlayerEvent;

public class PlayerLeft {
    @SubscribeEvent
    public void onPlayerLeave(PlayerEvent.PlayerLoggedOutEvent e) {
//		ServerQuestData.get();
//		Universe.get().getTeams().stream().filter((el) -> el.get);
//		get(e.player.getName());
//		Universe.get().getPlayer(e.player.getUniqueID()).getData();
//		writeData()
    }
}
